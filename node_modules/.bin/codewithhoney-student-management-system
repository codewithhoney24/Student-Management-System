#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir//Usr/bin/env" ]; then
  exec "$basedir//Usr/bin/env" node "$basedir/../codewithhoney-student-management-system/index.js" "$@"
else 
  exec /Usr/bin/env node "$basedir/../codewithhoney-student-management-system/index.js" "$@"
fi
